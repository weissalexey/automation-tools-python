# Automation Tools (Python)

A collection of Python scripts for automating routine tasks such as FTP backups, email sending, API requests, and XML parsing.

## 🚀 Scripts
- `backup_ftp.py` — Backup data from FTP server.
- `email_sender.py` — Send emails with attachments.
- `xml_parser.py` — Parse and transform XML files.
- `api_uploader.py` — Upload data via REST API.
- [Add more scripts here...]

## 🛠️ Usage
Run any script using:
```bash
python script_name.py
